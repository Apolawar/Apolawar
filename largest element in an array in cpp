#include<iostream>
using namespace std;
main()
{
    int arr[10],i,max;
    for(i = 0;i<10;i++){
        cout<<"enter "<<i+1<<" number: ";
        cin>>arr[i];
    }
    max=arr[0];
    for(i=0;i<10;i++){
        if(max<arr[i])
        {
            max=arr[i];
        }
}
